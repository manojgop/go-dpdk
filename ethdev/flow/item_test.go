package flow

import (
	"testing"

	_ "github.com/yerden/go-dpdk/common"
)

func TestItemRaw(t *testing.T) {
	// assert := common.Assert(t, true)

	// s := &ItemRaw{}
	// p := s.Set(&ItemRawConf{
	// Relative: true,
	// Search:   false,
	// Offset:   int32(10),
	// Limit:    uint16(20),
	// Pattern:  []byte{1, 2, 3, 4}})

	// assert(s == p, s)
}
